import React from "react";
import Modal from "./Modal";

const CreateColumnModal = ({onClose}) => {

    return (<Modal title="Create Column" onClose={onClose}>
        test
    </Modal>);
}

export default CreateColumnModal;